<div class="example-page">	
	<div class="row-fluid">
		<div class="span4">
			<% logo_image = image_tag("logo_small.png", :class => "logo") %>
			<%= link_to logo_image, "/"  %>		
		</div>	
		<div class="span8">	
			<br/><br/><br/>			
			<% if (current_user != nil) %>
				<div style="text-align:right;">
					<% sign_out_image = image_tag("back.png") %>
					<%= link_to sign_out_image, merchant_path(current_user) %>
				</div>
			<% end %>
		</div>
	</div>
	<%= image_tag("separator.png", :class => "separator") %>
	Use next PHP code snippet as example of intagration BitPal into your webshop:
    <div class="code">
			<xmp><?
// merchant id
$merchant_id = 1;

// order id
$order_id = 1;

// order description
$order_description = "New iPhone5";

// order amount
$amount = "10.96";

// type of output - json/html
$output_type = "html";

// your api key (look at your profile)
$api_key = "632db41b27fba2a5c569b0805e";

// url which should be shown after order is done 
$result_url = "http://yourdomain.com/result";

// url which will process requests from BitPal 
$postback_url = "http://yourdomain.com/postback";

// generate signature
$signature = hash("sha512", "$order_id:$api_key");

// payment form
print
   "<html>".
  "<form action='http://bitpal.se/pay' method=POST>".
  "<input type=hidden name='merchant_id' value=$merchant_id>".
  "<input type=hidden name='number' value=$order_id>".
  "<input type=hidden name='description' value=$order_description>".
  "<input type=hidden name='output_type' value=$output_type>".
  "<input type=hidden name='result_url' value=$result_url>".
  "<input type=hidden name='postback_url' value=$postback_url>".
  "<input type=hidden name='amount' value=$amount>".
  "<input type=hidden name='signature' value=$signature>".
  "<input type=submit value='Pay'>".
  "</form></html>";
?></xmp>
	</div>
	
<br/>	

<dl class="dl-horizontal">
  <dt>merchant_id</dt>
  <dd>— your unique identifier in BitPal payment system</dd>
  <dt>number</dt>
  <dd>— the unique identifier of order, generated by your e-shop</dd>
  <dt>description</dt>
  <dd>— the description of your order (not obligatory)</dd>
  <dt>amount</dt>  
  <dd>— amount of payment, price for one item or total amount for order</dd>  
  <dt>output_type</dt>
  <dd>— type of output: html or json. By default - html</dd>
  <dt>result_url</dt>
  <dd>— url which should be shown after order is done</dd>
  <dt>postback_url</dt>
  <dd>— url which will process requests from BitPal</dd> 
  <dt>signature</dt>
  <dd>— the sha512 hash for verifying</dd>    
</dl>
     
<br/>	
           
</div>
